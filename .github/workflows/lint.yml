name: Lint

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  lint:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 black
    
    - name: Run flake8
      continue-on-error: true
      run: |
        flake8 src/ api/ scripts/ tests/ --max-line-length=100 --ignore=E203,W503 --count --statistics || true
    
    - name: Check formatting with black
      continue-on-error: true
      run: |
        black --check src/ api/ scripts/ tests/ || true

